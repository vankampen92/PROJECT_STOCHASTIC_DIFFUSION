fprintf(fp, "\n");
fprintf(fp, "Input argument to control model definition\n");
fprintf(fp, "Notice that this is controled at the pre-compilation step through a #define variable. Both definitions should match!!!\n");

fprintf(fp, "Type of Model ---> -y0  %d\n", TYPE_of_MODEL);    /* 0 */
fprintf(fp, "Type of Model ---> -y2  %d\n", TYPE_of_NETWORK);    /* 2 */
